#!/bin/sh

if [ $# -eq 0 ]; then
    echo >&2 "$(basename -- "$0") <file>|'<glob>' [, ...]"
    exit 2
fi

oldWd=
while [ "$oldWd" != "$PWD" ]
do
    if eval ls "$@" >/dev/null 2>&1; then
	eval ls "$@" | sed -e "s+^+${PWD%/}/+"
    fi
    oldWd=$PWD
    cd .. || exit 3
done

exit 1
